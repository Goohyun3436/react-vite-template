import styled from "styled-components";

const Main = () => {
  return <MainBox className="page-wrapper">Main {import.meta.env.VITE_SERVER_IP}</MainBox>;
};

const MainBox = styled.div``;

export default Main;
